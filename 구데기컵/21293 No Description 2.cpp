#include <iostream>
using namespace std;

int main() {
    int z, y, x, w, v, u, t, s, r, p, o, n, m, l, k, j, i, h, g, f, e, d, c, b, a;


    cin >> z >> y >> x >> w >> v >> u >> t >> s >> r >> p >> o >> n >> m >> l >> k >> j
        >> i >> h >> g >> f >> e >> d >> c >> b >> a;

    cout << a - b << endl;

    cout << a * b << endl;

    cout << a + b + 1 << endl;

    return 0;
}
